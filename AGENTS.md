# Wardrobe project preferences

- The user has authorized publishing requested app changes to the existing Wardrobe website as part of completing those changes, until they explicitly withdraw that instruction or ask to take the website down (2026-09-06).
- Keep the website implementation in `site/` in sync with requested app functionality. Validate changes before publishing through Sites, using `site/.openai/hosting.json` as the source of the existing site identity.
- Preserve the existing approved audience and account isolation. This standing publication instruction does not authorize making the site public, expanding access, deleting user data, or changing the model provider.
- The AI model remains on the user's PC. Keep original Expo/local wardrobe data separate from website data.
